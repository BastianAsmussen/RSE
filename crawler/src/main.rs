mod crawler;

use std::collections::HashSet;

use anyhow::Result;
use crawler::Crawler;
use reqwest::Url;

#[tokio::main]
async fn main() -> Result<()> {
    // let (sender, receiver) = std::sync::mpsc::channel();

    loop {}

    Ok(())
}
